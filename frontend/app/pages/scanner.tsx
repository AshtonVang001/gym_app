import { ScrollView, Text } from "react-native";

const ScannerPage = () => {
    return(
        <ScrollView>
            <Text>Hello</Text>
        </ScrollView>
    )
}

export default ScannerPage;
